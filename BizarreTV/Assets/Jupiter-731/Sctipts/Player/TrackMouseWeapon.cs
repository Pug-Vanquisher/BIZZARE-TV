using System.Collections;
using System.Collections.Generic;
using UnityEngine;

namespace Jupiter731
{
    public class TrackMouseWeapon : MonoBehaviour
    {
        private Vector2 _mousePosition;
       // private Vector2 _
        private Vector3 _cameraPlayerOffset;

        private void Awake()
        {
            //_cameraPlayerOffset = transform.position - MainCamera.transform.position;
        }
    }
}
